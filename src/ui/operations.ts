import chalk from "chalk";

export async function operations(): Promise<null> {
  // console.clear();
  // console.log(
  //   `${name_params} = ${state_params.status} \n ${state_params.error_message}`
  // );
  return null;
}
