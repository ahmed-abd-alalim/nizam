import type { one_operation_state_type } from "../assets/type.js";

export async function ReactRouter(
  params: boolean
): Promise<one_operation_state_type> {
  const operation_state = { status: "", error_message: "" };
  return operation_state;
}
